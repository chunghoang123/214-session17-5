spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          add-label-locations: true
          search-locations: classpath:/config-repo
server:
  port: 8888

